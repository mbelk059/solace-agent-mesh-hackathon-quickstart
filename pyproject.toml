[project]
name = "sam-quickstart"
version = "0.1.0"
description = "Solace Agent Mesh quickstart template for hackathons"
requires-python = "==3.11.*"
dependencies = [
    "solace-agent-mesh==1.13.3",
    "PyGithub==2.8.1",
]
